Target from specification: 100 requests/second and 500 concurrent mock streams; gateway admission p95 <100ms. Run `LOAD_REQUESTS=500 uv run python tests/load/mock_load.py` against local HTTP services. This script measures a simultaneous burst, not sustained RPS. Latest evidence is `docs/evidence/load-500.json`: 500/500 completed, 13.9 completions/sec, admission p95 16,019.879ms, zero remaining reservations. The latency/throughput target is NOT met. Record RAM, CPU model and deployment tuning in a future controlled benchmark; this local run only records OS and logical CPU count.
